# CV Lab

Interactive computer-vision demonstrations that run entirely in the browser. The site has no backend, build step, or external runtime dependencies. The web app lets students explore image manipulation techniques discussed in the lectures.